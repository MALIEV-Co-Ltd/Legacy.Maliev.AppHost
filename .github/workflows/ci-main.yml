name: Main validation

on:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: apphost-main-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    uses: ./.github/workflows/_build-and-test.yml
